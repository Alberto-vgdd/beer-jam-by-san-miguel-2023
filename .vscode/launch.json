{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Unity Editor (VSTU)",
            "type": "vstu-unity",
            "request": "attach",
            "projectPath": "${workspaceFolder}"
        },
        {
            "name": "Attach to Unity",
            "type": "vstuc",
            "request": "attach"
        }
    ]
}